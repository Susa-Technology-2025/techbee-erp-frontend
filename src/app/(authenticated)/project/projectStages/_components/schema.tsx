import {z} from "zod"
export const ProjectStageFormSchema = z.object({ id: z.string().optional().nullable(), name: z.string({ error: "Project stage name is required" }), code: z.string({ error: "Code is required" }), sequence: z.coerce.number({ error: "Sequence is required" }), active: z.boolean({ error: "Active status is required" }).default(true), requiresApproval: z.boolean({ error: "Approval requirement is required" }).default(false), setField: z.object({ id: z.string({ error: "Set field is required" }) }).meta({ url: "/projectStageSets" }), color: z.string().optional().nullable(), description: z.string().optional().nullable(), notes: z.string().optional().nullable(), triggersNotification: z.boolean().optional().nullable().default(false) })