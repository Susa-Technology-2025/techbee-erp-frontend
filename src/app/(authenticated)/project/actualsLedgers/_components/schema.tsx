import {z} from "zod"
export const ActualsLedgerFormSchema = z.object({ id: z.string().optional().nullable(), amount: z.coerce.number({ error: "Amount is required" }), category: z.enum([ "Labor", "Materials", "Equipment", "Subcontract", "Travel", "Misc", ]).optional().nullable(), currency: z.enum([ "ETB", "USD", "EUR", "GBP", "AED", ], { error: "Currency is required" }), createdBy: z.string().optional().nullable(), date: z.coerce.date({ error: "Date is required" }), description: z.string().optional().nullable(), isBillable: z.boolean({ error: "Is billable is required" }).default(true), isInvoiced: z.boolean({ error: "Is invoiced is required" }).default(false), sourceId: z.string({ error: "Source ID is required" }), sourceType: z.string({ error: "Source type is required" }), wbsItem: z.object({ id: z.string(), }, { error: "Wbs item is required" }).meta({ url: "/wbsItems" }), })